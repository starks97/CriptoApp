import * as React from 'react';
import { Error} from '../helpers/usefullThings'
 function ErrorMessage () {
  return (
    <div>
      <Error>All the fields must be select!</Error>
    </div>
  );
    }

export default ErrorMessage;